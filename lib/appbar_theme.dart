import 'package:flutter/material.dart';
import 'constants.dart';

class AppBarTheme extends StatelessWidget {
  AppBarTheme();
  @override
  Widget build(BuildContext context) {
    return Container(
      decoration: BoxDecoration(gradient: kColorGradient),
    );
  }
}
